# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment variables
.env

# RAG Database & Documents (too large for git)
chroma_db/
documents/*.pdf
rag_metadata.json
uv.lock

# Knowledge base (PDFs are too large for git)
knowledge_base/
backups/
archive/

# Crawl logs (too large for git)
*.log
chamoru_crawl.log
chamoru_extended_crawl.log

# Conversation logs (sensitive user data)
conversation_logs.jsonl
conversation_logs_*.jsonl

# OS Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Note: dictionary_data/ is NOT ignored - needed for vocabulary API
# The JSON files (~6MB total) are required for the dictionary service

# Evaluation test outputs (generated, not tracked)
evaluation/tmp/
evaluation/comparison_results/
evaluation/comparison_output.txt
evaluation/prod_test_*.txt
evaluation/skill_test_*.txt
evaluation/eval_results_*.json
evaluation/eval_report_*.txt
evaluation/full_test_output.txt
# Keep: test_queries_v3.json, test_evaluation.py, run_comparison.py, BASELINE_METRICS.md
